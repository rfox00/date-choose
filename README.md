# date-choose
通过鼠标划过区域，选择对应日期

echo # date-choose >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/rfox00/date-choose.git
git push -u origin master
